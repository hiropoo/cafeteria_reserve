import 'package:flutter/material.dart';

class PenaltyPage extends StatelessWidget {
  const PenaltyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('ペナルティがあります'),
    );
  }
}
